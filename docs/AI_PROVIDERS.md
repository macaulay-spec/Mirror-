# AI Providers

Jarvis uses the Backend as the AI routing gateway.
The default provider is **Gemini**.

## Supported Providers
Currently supported via the backend:
- Gemini
- OpenAI
- Anthropic

The Android app specifies the provider in the payload to `/api/v1/ai/chat`.
